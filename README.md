# blink

A URL shortener built with Go, PostgreSQL, and Redis.

## Run with Docker

Make sure you have [Docker](https://www.docker.com/) installed, then run:

```bash
docker compose up --build
```

That's it. 

## Status

Work in Progress!
